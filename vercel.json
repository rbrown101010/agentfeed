{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd backend && npm install --omit=dev --ignore-scripts && cd ../web && npm install && npm run build",
  "outputDirectory": "web/dist",
  "framework": null
}